// Dados primitivos -> string, number, undefined, null, boolean
const nome = 'Thiago'; // string
const nome1 = 'Thiago'; // string
const nome2 = 'Thiago'; // string
const num1 = 10; // number
const num2 = 10.52; // number
let nomeAluno; // undefined -> Não aponta para local nenhum na memória
const sobrenomeAluno = null; // Nulo -> Não aponta para local nenhum na memória ( interage com usuário )
const aprovado = true; // boolean -> true ou false ( lógico )

const a = [1, 2];
const b = a;
